# Markdown CLI Writer Configuration
# Default output directory
OUTPUT_DIR=./output

# Default file format (md, json, html)
DEFAULT_FORMAT=md

# Default syntax style (current, prd, mixed)
DEFAULT_SYNTAX=current

# Enable debug mode
DEBUG=false

# Default filename prefix
FILENAME_PREFIX=

# Auto-save generated files
AUTO_SAVE=true
